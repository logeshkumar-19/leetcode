class Solution {
    public boolean isPalindrome(int x) {
        int n =x;
        int sum=0;
        while(x>0){
            int m=x%10;
            sum = sum*10+m;
            x/=10;
        }
        return sum==n;
        
    }
}
